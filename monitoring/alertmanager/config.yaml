global:
  slack_api_url: 

route:
  receiver: 'slack'

receivers:
  - name: 'slack'
    slack_configs:
     - channel: '#ops'
       send_resolved: true
       title_link: 'http://localhost:9093/#/alerts?receiver={{ .Receiver | urlquery }}'
