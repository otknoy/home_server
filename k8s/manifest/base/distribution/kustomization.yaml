apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: distribution

resources:
  - ./namespace.yaml
  - ./registry.yaml

configMapGenerator:
  - name: registry-cm
    literals: []
