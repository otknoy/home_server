apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - alertmanager.yaml
  - alertmanager-slack-url-secret.yaml

configMapGenerator:
  - name: alertmanager-cm
    files:
      - config/alertmanager.yml
